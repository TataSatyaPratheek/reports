# PDF Analyzer Project Dependencies
# Pin versions for stability and security. Check for updates periodically.

streamlit==1.44.0
nltk==3.8.1
spacy==3.8.0
openparse==0.7.0 
sentence-transformers==2.7.0
numpy==1.26.4
chromadb==0.5.3
ollama==0.2.1
psutil==5.9.8
pydantic>=2.0.0,<3.0.0 # Add this line to enforce Pydantic v2

# Note: The spaCy model 'en_core_web_sm' is downloaded separately via a spacy command.
# Ensure the version downloaded matches the spacy version if necessary (e.g., 3.7.x).
